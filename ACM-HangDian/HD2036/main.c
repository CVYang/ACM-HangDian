#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n=0,i=0;
    int length[100];
    while (scanf("%d",&n)!=0)
    {
        for (i=0;i<n;i++)
        {
            scanf("%d",&length[i])
        }

    }
}
