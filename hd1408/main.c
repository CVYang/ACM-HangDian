#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i=0,n=0,step=0,sum=0;
    while(scanf("%d %d",&n,&step)!=EOF)
    {
        ///n = n%step > 0 ? n/step+1 : n/step;
        for(i=0;i<n/step;i++)
        {

        }
    }
    return 0;
}
